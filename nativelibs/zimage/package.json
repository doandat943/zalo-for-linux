{
  "name": "re-impl-zimage",
  "version": "1.0.0",
  "description": "Re-impl of zimage for Linux",
  "main": "zimage.node",
  "scripts": {
    "build": "cargo build --release && cp target/release/libzimage.so zimage.node",
    "build:debug": "cargo build",
    "test": "node test.js"
  },
  "author": "DMKha",
  "license": "ISC",
  "files": [
    "index.js",
    "*.node"
  ]
}
